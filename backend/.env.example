# MongoDB Connection URI (Replace with your actual MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/srplastic?retryWrites=true&w=majority

# Port for backend server (default is 5000)
PORT=5001
